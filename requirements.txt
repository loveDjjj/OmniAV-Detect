# Core MVAD preprocessing
tqdm

# Training with Qwen Omni via ms-swift
ms-swift
qwen-omni-utils
qwen-vl-utils

# Optional model/runtime dependencies are managed by the server conda environment.
# Keep torch / transformers / peft versions aligned with the installed ms-swift stack.
